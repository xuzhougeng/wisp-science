{
  "$schema": "https://schema.tauri.app/config/2",
  "build": {
    "beforeDevCommand": { "script": "trunk serve --address 127.0.0.1 --port 1421 --dist dist-dev", "cwd": "../ui" },
    "beforeBuildCommand": { "script": "trunk build --release --dist dist", "cwd": "../ui" }
  }
}
